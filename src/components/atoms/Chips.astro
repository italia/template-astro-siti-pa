---
type Props = {
  label: string;
  variant?: "primary" | "standard";
};
const { label, variant = "standard" } = Astro.props;

const className = variant === "primary" ? "chip-primary " : "";
---

<div class={`${className} chip chip-simple`}>
  <span class="chip-label">{label}</span>
</div>
