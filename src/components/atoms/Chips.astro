---
type Props = {
  label: string;
  variant?: "primary" | "standard";
};
const { label, variant = "standard" } = Astro.props;

const style = variant === "primary" ? "chip-primary " : "";
---

<div class={`${style} chip chip-simple`}>
  <span class="chip-label">{label}</span>
</div>
