export type ChipLinkProps = {
  label: string;
  visuallyHidden: string;
  linkTo: string;
  disabled?: boolean;
  variant?: "primary" | "standard" | "outline-white";
  size?: "default" | "large";
};
