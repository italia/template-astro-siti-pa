---
import { CardInfoAndService } from "@components/molecules/CardInfoAndService";
import type { ExternalLinkFragmentType } from "@graphql/fragment/commonFragments";

type DatoRecord = ExternalLinkFragmentType & {
  __typename: string;
  id: string;
};

type Props = {
  block: DatoRecord;
};

const { block } = Astro.props;

if (!block.label || !block.url) {
  return null;
}
---

<CardInfoAndService
  title={block.label}
  linkTo={block.url}
  description={block.description || ""}
/>
