export type ShareDropdownProps = {
  /** URL da condividere (es. il link del video) */
  url: string;
  /** Titolo del contenuto, usato nei testi di condivisione e nell'aria-label */
  title: string;
  /** Etichetta del pulsante */
  label?: string;
};
