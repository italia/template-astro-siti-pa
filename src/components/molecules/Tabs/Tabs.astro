---
import TabsItem from "./TabsItem.astro";
import "./style.scss";
import type { TabItemProps } from "./types";
type Props = {
  items: TabItemProps[];
};
const { items } = Astro.props;
---

<ul class="nav nav-tabs bg-primary border-bottom-0">
  {items.map((item) => <TabsItem {...item} />)}
</ul>
