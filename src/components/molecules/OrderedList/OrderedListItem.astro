---
import { NumberContainer } from "@components/atoms/NumberContainer";
import type { ListItemProps, VariantListProps } from "./types";

type Props = ListItemProps & {
  index: string;
  variant: VariantListProps;
};
const { title, paragraph, index, variant } = Astro.props;

const titleClass = variant === "small" ? "fs-4 m-0 fw-semibold" : "fs-3";
const textClass = variant === "small" ? "text-secondary" : "";
---

<style>
  @import "./style.scss";
</style>

<li class="mb-4">
  <div class="item-row align-items-top">
    <div class="it-rounded-icon">
      <NumberContainer content={index.toString()} />
    </div>
    <div class="item-text">
      <div>
        <h3 class=`${titleClass} text-dark`>{title}</h3>
        <div class=`${textClass} m-0` set:html={paragraph} />
      </div>
    </div>
  </div>
</li>
