---
import type { SectionBackground } from "@components/layout/SplitContent/SplitContent.astro";
import type { HeroProps } from "./types";
import { Image } from "@components/atoms/Image";
type Props = HeroProps;

const {
  title,
  paragraph,
  image,
  showBreadcrumb,
  backgroundColor = "default",
} = Astro.props;

const backgroundColorClassNames: Record<SectionBackground, string> = {
  default: "",
  primary: "bg-primary",
  dark: "primary-bg-a10",
  lighter: "lightgrey-bg-a3 dark",
};
---

<style>
  @import "./style.scss";
</style>
<section
  class=`it-hero-wrapper xsmall-size ${backgroundColorClassNames[backgroundColor]}`
>
  <div class="container">
    <div class="row align-items-center">
      <div class="col-12 col-lg-8">
        <div class="it-hero-text-wrapper">
          {showBreadcrumb && <slot name="breadcrumb" />}
          <h1>{title}</h1>
          {
            paragraph && (
              <p class="d-none d-lg-block font-sans-serif">{paragraph}</p>
            )
          }
        </div>
      </div>

      <div class="col-12 col-lg-4">
        <div class="img-container">
          <div class="ratio ratio-4x3">
            <Image {...image} />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
