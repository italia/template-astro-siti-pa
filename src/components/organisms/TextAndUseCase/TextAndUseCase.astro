---
import TextContainer from "../../molecules/TextContainer/TextContainer.astro";
import { UseCaseCard } from "../../molecules/UseCaseCard";
import type { useCaseCardProps } from "../../molecules/UseCaseCard/types";
import "./style.scss";

type Props = {
  title: string;
  paragraph: string;
  labelButton: string;
  useCases: useCaseCardProps[];
};
const { title, paragraph, labelButton, useCases } = Astro.props;
---

<section>
  <div class="container">
    <div class="row align-items-top justify-content-between py-80 py-lg-6">
      <div class="col-lg-5 col-12">
        <TextContainer
          title={title}
          paragraph={paragraph}
          labelButton={labelButton}
        />
      </div>
      <div class="col-lg-6 col-12">
        <p class="fw-semibold fs-6">IN EVIDENZA</p>
        <div class="usecase-container">
          {useCases.map((useCase) => <UseCaseCard {...useCase} />)}
        </div>
      </div>
    </div>
  </div>
</section>
