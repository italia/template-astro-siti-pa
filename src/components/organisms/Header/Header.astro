---
import HeaderCentral from "./HeaderCentral.astro";
import HeaderSlim from "./HeaderSlim.astro";
import type { HeaderProps } from "./types";

type Props = HeaderProps;

const {
  slimContent,
  locales,
  brandTitle,
  brandTagLine,
  menuItems,
  linkBackHome,
  logo,
} = Astro.props;
---

<header class="it-header-wrapper shadow-sm">
  <HeaderSlim slimContent={slimContent} linkTo={"#"} locales={locales} />
  <HeaderCentral
    brandTitle={brandTitle}
    brandTagLine={brandTagLine}
    menuItems={menuItems}
    linkBackHome={linkBackHome}
    logo={logo}
  />
</header>
